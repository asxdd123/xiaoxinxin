server:
  port: 8000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/aaa3?serverTimezone=GMT%2B8&useSSL=true
    username: root
    password: itheima

mybatis:
  type-aliases-package: mybatis.entry
  mapper-locations: classpath:mapper/*.xml